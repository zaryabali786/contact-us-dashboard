export interface EmailAddress {
  id: string | number;
  contact_id?: string | number;
  contactId?: string | number;
  email: string;
  type?: string;
  is_primary?: boolean;
  isPrimary?: boolean;
}
